samenvatting_2025-08-09 19:20

feat: Implementeer uitloggen & paletbeheer

Voegt twee belangrijke functionaliteiten toe aan het beheerderspaneel en verbetert de data-export:

1.  **Logout Functionaliteit:**
    -   Een uitlogknop is toegevoegd aan de hoofdinterface voor ingelogde gebruikers.
    -   Deze knop beëindigt de sessie door de authenticatiegegevens uit de lokale opslag te verwijderen en de UI-status bij te werken.

2.  **Paletbeheer:**
    -   Gebruikers kunnen nu nieuwe kleurenpaletten aanmaken via de "Appearance" sectie.
    -   Op maat gemaakte (niet-standaard) paletten kunnen worden verwijderd na een bevestigingsdialoog.
    -   De logica is geïmplementeerd in de `useAppData` hook om de datastructuur consistent te houden.

fix: Robuuste data export & import logica

Lost een kritieke bug op waarbij de data-export een leeg of incorrect JSON-bestand opleverde.

-   **Backend:** De transformatiefunctie in de Cloud Function (`functions/index.js`) is herschreven om robuuster om te gaan met mogelijk ontbrekende data in de database, zodat de export altijd een correct gestructureerd object teruggeeft.
-   **Frontend:** De `handleExport` functie in `App.tsx` is gecorrigeerd om de ontvangen JSON-data correct te verwerken tot een downloadbaar bestand. De overbodige `JSON.stringify` op een al correct object is verwijderd.
-   De import- en export-endpoints op de backend zijn nu volledig geïmplementeerd.

Bugfix: De naam van een AI-gegenereerd thema wordt nu correct overgenomen wanneer een bestaand palet wordt overschreven.